<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width">
        <title>Tech Blog</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
         <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" 
        rel="stylesheet" 
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" 
        crossorigin="anonymous"/>
        <link rel="stylesheet" href="/css/style.css"/>

        <body class="flex-column min-100-vh">
            <nav class="navbar custom-navbar navbar-expand-lg fixed-top">
                <div class="container">
                    <div>
                        <a href="/" class="nav-item nav-link"><i class="bi bi-house"></i>Home</a>
                        {{#if loggedIn}}
                        <a href="/dashboard" class="nav-item nav-link"><i class="bi bi-card-list"></i>Dashboard</a>
                        {{else}}
                        <a href="/login" class="nav-item nav-link"><i class="bi bi-box-arrow-in-right"></i>Login</a>
                        {{/if}}
                        {{!-- {{#if loggedIn}} --}}
                        <a href='/' class="nav-item nav-link" id="logout"><i class="bi bi-box-arrow-out-right">Logout</i></a>
                        {{!-- {{/if}} --}}
                    </div>
                </div>
            </nav>
            <header class="text-center">
                <div class="container d-flex align-items-center flex-column">
                    <h1 id="header">Welcome to Tech Blog</h1>
                </div>
            </header>
            <main class="col-auto">
                {{{body}}}
            </main>
        </body>
    </head>
</html>

<script src="/js/logout.js"></script>